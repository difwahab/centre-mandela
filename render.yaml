services:
  - type: web
    name: my-app-server
    env: node
    plan: free
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: ./data/database.sqlite
      - key: VITE_API_URL
        value: /api
      - key: NODE_VERSION
        value: 20.11.1
    buildFilter:
      paths:
        - client/**
        - server/**
        - shared/**
