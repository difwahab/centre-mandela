# URL de l'API — accessible côté client via import.meta.env.VITE_API_URL
VITE_API_URL=http://localhost:5000/api

# URL de la base de données SQLite pour Drizzle ORM
DATABASE_URL=file:data/database.sqlite

# Clé secrète pour les sessions Express
SESSION_SECRET=s3cureSuperSecretKey123!  # ⚠️ Change-la en production (valeur longue et aléatoire)

# (optionnel) Port pour Express (modifiable si besoin)
PORT=5000
